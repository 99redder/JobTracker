name: Deploy JobTracker to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate runtime Firebase config
        run: |
          cat > firebase-config.local.js <<'EOF'
          window.JOBTRACKER_FIREBASE_CONFIG = {
            apiKey: "${{ secrets.JT_FIREBASE_API_KEY }}",
            authDomain: "${{ secrets.JT_FIREBASE_AUTH_DOMAIN }}",
            projectId: "${{ secrets.JT_FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ secrets.JT_FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ secrets.JT_FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ secrets.JT_FIREBASE_APP_ID }}"
          };
          EOF

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
